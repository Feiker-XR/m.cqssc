var express = require('express');
var router = express.Router();

/* GET users listing. */
(function(){
  var tepm
})();


router.get('/queryUserBal', function(req, res, next) {
  res.send('829856');
});

module.exports = router;
